#!/usr/bin/env python3
import sys
import fund_my_watcard

if sys.version_info[0] == 3:

    if __name__ == "__main__":
        fund_my_watcard.main()
else:

    fund_my_watcard.report_error("Python3 is required, try to run this script by using 'python3 watcard'.")
